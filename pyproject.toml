[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "spidercam"
version = "0.1.0"
description = "SpiderCam Autónoma Guiada por Visión Artificial"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
